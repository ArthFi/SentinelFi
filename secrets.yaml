# CRE Secrets Manifest — target-independent, flat format
# Maps secret IDs (used by runtime.getSecret()) to environment variable names
# Actual values are loaded from .env at project root during simulation

secretsNames:
  GEMINI_API_KEY:
    - GEMINI_API_KEY
